name: termix
description: >-
  A dependency-free toolkit for building interactive terminal UIs in Dart:
  an MVU runtime, styling, color and width handling.
version: 0.0.1

environment:
  sdk: ^3.5.0

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.0
