# termix

A dependency-free toolkit for building interactive terminal UIs in Dart: an
MVU runtime, styling, color and width handling.

> **0.0.1 is a placeholder** reserving the package name. The real API is on its
> way.
